node_modules/
tools/

# Firebase Functions secrets / local env — never commit the API key
functions/serviceAccount.json
functions/.secret.local
.env
.env.*
*.local

# Local dev / emulator artifacts
# .firebase/ holds per-machine deploy caches. Tracking them means every
# teammate's deploy rewrites the same file and collides with everyone else's.
.firebase/
.static-server.cjs
maps-key-test.html
firebase-debug.log
firebase-debug.*.log
*-debug.log
